# BookStore

This is full-stack solution, separated to two projects: frontend (client) and the backend (server).

## To run the backend

```shell
npm i
npm start
```

## To test the backend

```shell
npm i
npm test
```
